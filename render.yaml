# Use this file when deploying to Render via repo import
services:
  - type: web
    name: clipsbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
